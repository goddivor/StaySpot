import React from 'react';

const Dashboard: React.FC = () => {
    return (
        <div style={{ textAlign: 'center', marginTop: '2rem' }}>
            <h1>📊 User Dashboard</h1>
            <p>Hi, this will be the user dashboard page incoming!</p>
        </div>
    );
};

export default Dashboard;