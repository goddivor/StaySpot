import React from 'react';

const Favorites: React.FC = () => {
    return (
        <div style={{ textAlign: 'center', marginTop: '2rem' }}>
            <h1>❤️ Favorites</h1>
            <p>Hi, this will be the favorites page incoming!</p>
        </div>
    );
};

export default Favorites;