import React from 'react';

const Profile: React.FC = () => {
    return (
        <div style={{ textAlign: 'center', marginTop: '2rem' }}>
            <h1>👤 User Profile</h1>
            <p>Hi, this will be the user profile page incoming!</p>
        </div>
    );
};

export default Profile;