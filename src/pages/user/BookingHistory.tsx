import React from 'react';

const BookingHistory: React.FC = () => {
    return (
        <div style={{ textAlign: 'center', marginTop: '2rem' }}>
            <h1>📅 Booking History</h1>
            <p>Hi, this will be the booking history page incoming!</p>
        </div>
    );
};

export default BookingHistory;