import React from 'react';

const Login: React.FC = () => {
    return (
        <div style={{ textAlign: 'center', marginTop: '2rem' }}>
            <h1>🔐 Login Page</h1>
            <p>Hi, this will be the login page incoming!</p>
        </div>
    );
};

export default Login;