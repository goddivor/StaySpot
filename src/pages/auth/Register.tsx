import React from 'react';

const Register: React.FC = () => {
    return (
        <div style={{ textAlign: 'center', marginTop: '2rem' }}>
            <h1>📝 Register Page</h1>
            <p>Hi, this will be the register page incoming!</p>
        </div>
    );
};

export default Register;