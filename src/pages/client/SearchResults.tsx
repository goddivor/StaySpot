import React from 'react';

const SearchResults: React.FC = () => {
    return (
        <div style={{ textAlign: 'center', marginTop: '2rem' }}>
            <h1>🔍 Search Results Page</h1>
            <p>Hi, this will be the search results page incoming!</p>
        </div>
    );
};

export default SearchResults;
