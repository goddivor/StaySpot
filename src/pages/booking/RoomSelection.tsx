import React from 'react';

const RoomSelection: React.FC = () => {
    return (
        <div style={{ textAlign: 'center', marginTop: '2rem' }}>
            <h1>🛏️ Room Selection</h1>
            <p>Hi, this will be the room selection page incoming!</p>
        </div>
    );
};

export default RoomSelection;