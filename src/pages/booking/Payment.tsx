import React from 'react';

const Payment: React.FC = () => {
    return (
        <div style={{ textAlign: 'center', marginTop: '2rem' }}>
            <h1>💳 Payment</h1>
            <p>Hi, this will be the payment page incoming!</p>
        </div>
    );
};

export default Payment;