import React from 'react';

const GuestDetails: React.FC = () => {
    return (
        <div style={{ textAlign: 'center', marginTop: '2rem' }}>
            <h1>👥 Guest Details</h1>
            <p>Hi, this will be the guest details page incoming!</p>
        </div>
    );
};

export default GuestDetails;